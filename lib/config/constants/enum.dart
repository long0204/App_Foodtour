enum ModeType { group, couple }

enum ReqStatus { init, loading, success, edit, fail, empty, loadMore }

enum AddressType { home, office }

enum RankType { iron, brown, sliver, gold, diamond }

enum CmsType { endow, doc }

enum CategoryType { pharmacy, doc }

enum UserMode { guest, authenticated }

enum SocialType { none, google, apple }

enum FireworksOrigin { centerAroundPopup, leftEdge, rightEdge, topEdge, bottomEdge, }
